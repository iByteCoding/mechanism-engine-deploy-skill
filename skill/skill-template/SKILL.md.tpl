---
name: {机制名称}部署
description: 在目标模块中部署{机制名称}，实现{机制核心能力描述}
---

# {机制名称}部署

## 一、实体概览

| 实体类型 | 识别特征 | 典型场景 | 核心接口/基类 | 关联字段 | 示例类名 |
|----------|----------|----------|---------------|----------|----------|
| {类型1} | {特征描述} | {场景描述} | {接口/基类} | {字段列表} | {Module}{Entity} |
| {类型2} | {特征描述} | {场景描述} | {接口/基类} | {字段列表} | {Module}{Entity} |

**关键约束**

- {约束1}
- {约束2}

---

## 二、部署流程

### 2.1 Maven 依赖

```xml
<!-- {机制名称} 核心依赖 -->
<dependency>
    <groupId>{groupId}</groupId>
    <artifactId>{artifactId}</artifactId>
    <version>${{framework.version}}</version>
</dependency>
```

### 2.2 执行部署

| 步骤 | 名称 | 参考文档 | 依赖 | 说明 |
|------|------|----------|------|------|
| 1 | {步骤名称} | [ref-01-xxx-spec.md](references/ref-01-xxx-spec.md) | - | {步骤说明} |
| 2 | {步骤名称} | [ref-02-xxx-integration.md](references/ref-02-xxx-integration.md) | 步骤1 | {步骤说明} |

---

## 四、部署验证清单

- [ ] {验证点1}
- [ ] {验证点2}
- [ ] {验证点3}

---

## 五、代码注释规范

| 代码位置 | 注释要求 |
|----------|----------|
| 接口方法 | Javadoc，说明参数、返回值、异常 |
| Service 公开方法 | Javadoc，说明业务逻辑和注意事项 |
| Entity 字段 | 非自解释字段须注明含义和约束 |
| Controller 端点 | 说明接口用途、权限要求 |
| 类级别 | 说明类的职责和使用场景 |

**行内注释原则**

- 说明"为什么"而非"做什么"
- 复杂逻辑须注释原因和背景
- 避免无意义的翻译式注释

---

## 六、占位符说明

| 占位符 | 含义 | 示例值 |
|--------|------|--------|
| `{Module}` | 类名前缀（大写驼峰） | `User` |
| `{module}` | 包名/变量名（小写驼峰） | `user` |
| `{模块Key}` | 业务标识键（大写下划线） | `USER` |
| `{modulePrefix}` | 表名前缀 | `usr_` |
| `{moduleSuffix}` | 表名后缀 | `_v1` |
